apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: roof-hunter

resources:
  - namespace.yaml
  - web-deployment.yaml
  - api-deployment.yaml
  - ingress.yaml
  - sync-cronjob.yaml
